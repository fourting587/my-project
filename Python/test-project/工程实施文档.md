# 数据处理/分析平台工程实施文档

## 项目概述

基于Python + FastAPI + MySQL的数据处理/分析平台，支持API数据抓取、结构化数据存储、全文检索、大数据量计算分析及可视化展示。

**硬件配置：**
- CPU：4核
- 内存：8GB
- 磁盘：160GB

**资源配置策略（总计约6.5GB）：**
- MySQL：1GB（可配置主从）
- Elasticsearch：2GB（1GB堆内存）
- FastAPI应用：1GB（4 workers）
- Redis：512MB
- RabbitMQ：512MB
- Prometheus+Grafana：512MB
- 系统预留：1.5GB

---

## 技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          数据层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │   MySQL      │  │   Redis      │  │   Elasticsearch      │   │
│  │  1GB内存     │  │  512MB内存   │  │    2GB内存           │   │
│  │  主从可选    │  │  持久化开启  │  │    1GB堆内存         │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                          服务层                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                  FastAPI Application                      │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │   │
│  │  │ API模块   │ │数据处理  │ │ 定时任务  │ │ 全文搜索     │  │   │
│  │  │ 1GB内存   │ │ 500条批次 │ │ 4 workers │ │ ES 2GB       │  │   │
│  │  │ 4 workers │ │ Pandas    │ │ Celery    │ │              │  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                          消息层                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    RabbitMQ 512MB                         │   │
│  │              管理界面:15672  队列持久化                   │   │
│  └──────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                          展示层                                  │
│  ┌─────────────────────┐  ┌─────────────────────────────────┐   │
│  │   Web管理界面        │  │         Grafana监控              │   │
│  │  (Vue3 + ECharts)   │  │   512MB内存 + 数据可视化         │   │
│  └─────────────────────┘  └─────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                          监控层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ Prometheus   │  │  Alertmanager│  │    邮件通知           │   │
│  │  指标采集     │  │  告警管理     │  │                     │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                      160GB磁盘管理策略                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ 60天数据保留  │  │ 自动清理脚本  │  │  日志轮转14天        │   │
│  │ 支持大数据量  │  │ 磁盘告警85%  │  │  备份保留7份         │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

---

## 第一阶段：基础环境搭建

### 1.1 虚拟机配置

**VMware虚拟机规格：**
- CPU：4核
- 内存：8GB
- 磁盘：160GB
- 网络：NAT或桥接模式

### 1.2 Ubuntu 24.04 系统初始化

```bash
# 系统更新
sudo apt update && sudo apt upgrade -y

# 安装基础工具
sudo apt install -y vim git curl wget net-tools htop docker-compose

# 设置时区
sudo timedatectl set-timezone Asia/Shanghai

# 配置SSH（建议修改默认端口）
sudo vim /etc/ssh/sshd_config
# Port 2222
# PermitRootLogin no
sudo systemctl restart sshd

# 创建项目目录
mkdir -p /data/{app,mysql,redis,es,rabbitmq,prometheus,grafana,logs,backups}
```

### 1.3 Docker环境安装

```bash
# 安装Docker
sudo apt install -y docker.io
sudo systemctl enable docker && sudo systemctl start docker

# 安装Docker Compose
sudo apt install -y docker-compose-v2

# 添加用户到docker组
sudo usermod -aG docker $USER
```

---

## 第二阶段：基础架构部署

### 2.1 Docker Compose配置

```yaml
# docker-compose.yml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ./mysql/data:/var/lib/mysql
      - ./mysql/my.cnf:/etc/mysql/conf.d/my.cnf
    ports:
      - "3306:3306"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    networks:
      - backend

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: >
      redis-server
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
    volumes:
      - ./redis/data:/data
    ports:
      - "6379:6379"
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend

  elasticsearch:
    image: elasticsearch:8.11.0
    container_name: elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
    volumes:
      - ./es/data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
    networks:
      - backend

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    volumes:
      - ./rabbitmq/data:/var/lib/rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend

  app:
    build: ./app
    container_name: data-platform-app
    restart: unless-stopped
    environment:
      - APP_ENV=production
      - WORKERS=4
      - MAX_CONNECTIONS=100
      - PROCESS_BATCH_SIZE=500
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
      - ES_HOST=elasticsearch
      - RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    ports:
      - "8000:8000"
    networks:
      - backend

  celery-worker:
    build: ./app
    container_name: celery-worker
    restart: unless-stopped
    command: celery -A app.tasks worker --loglevel=info --concurrency=4
    environment:
      - APP_ENV=production
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
      - ES_HOST=elasticsearch
      - RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    networks:
      - backend

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus:/etc/prometheus
      - ./prometheus/data:/prometheus
    ports:
      - "9090:9090"
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M
    networks:
      - backend

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin123
    volumes:
      - ./grafana/data:/var/lib/grafana
    ports:
      - "3000:3000"
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M
    networks:
      - backend

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/html:/usr/share/nginx/html
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
    networks:
      - backend

networks:
  backend:
    driver: bridge
```


### 2.2 MySQL 配置

```ini
# mysql/my.cnf
[mysqld]
# 内存优化（1GB总内存）
innodb_buffer_pool_size = 512M
innodb_log_buffer_size = 16M
innodb_log_file_size = 128M
key_buffer_size = 32M
query_cache_size = 32M
query_cache_limit = 4M
tmp_table_size = 32M
max_heap_table_size = 32M

# 连接配置
max_connections = 50
max_connect_errors = 1000
wait_timeout = 600
interactive_timeout = 600

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# 性能优化
innodb_flush_log_at_trx_commit = 2
innodb_flush_method = O_DIRECT
innodb_file_per_table = 1

# 慢查询日志
slow_query_log = 1
slow_query_log_file = /var/lib/mysql/slow.log
long_query_time = 2

[client]
default-character-set = utf8mb4
```

### 2.3 Elasticsearch 配置

```yaml
# es/elasticsearch.yml
cluster.name: data-platform
node.name: es-single
path.data: /usr/share/elasticsearch/data
path.logs: /usr/share/elasticsearch/logs

# 单节点配置
discovery.type: single-node
cluster.initial_master_nodes: ["es-single"]

# 内存和性能
bootstrap.memory_lock: false
indices.memory.index_buffer_size = 256mb
indices.queries.cache.size = 128mb
indices.fielddata.cache.size = 128mb

# 分片和副本（单节点优化）
index.number_of_shards: 1
index.number_of_replicas: 0

# 磁盘水位线（160GB磁盘）
cluster.routing.allocation.disk.watermark.low: 85%
cluster.routing.allocation.disk.watermark.high: 90%
cluster.routing.allocation.disk.watermark.flood_stage: 95%

# 搜索优化
search.max_open_scroll_context: 100
```

### 2.4 Nginx 配置

```nginx
# nginx/nginx.conf
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    # 性能优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    # 限流配置
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/m;

    upstream app {
        server app:8000 max_fails=3 fail_timeout=30s;
    }

    server {
        listen 80;
        server_name localhost;

        # 静态文件
        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
        }

        # API代理
        location /api/ {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://app/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        # WebSocket支持（用于实时监控）
        location /ws/ {
            proxy_pass http://app/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # 健康检查
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
```

### 2.5 Prometheus 配置

```yaml
# prometheus/prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'fastapi-app'
    static_configs:
      - targets: ['app:8000']
    metrics_path: '/metrics'

  - job_name: 'mysql'
    static_configs:
      - targets: ['mysql:3306']

  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']

  - job_name: 'elasticsearch'
    static_configs:
      - targets: ['elasticsearch:9200']

  - job_name: 'rabbitmq'
    static_configs:
      - targets: ['rabbitmq:15692']
```

```yaml
# prometheus/alert_rules.yml
groups:
  - name: system_alerts
    rules:
      - alert: HighMemoryUsage
        expr: (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100 > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "内存使用率过高"
          description: "内存使用率超过85%"

      - alert: HighDiskUsage
        expr: (1 - (node_filesystem_avail_bytes / node_filesystem_size_bytes)) * 100 > 85
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "磁盘使用率过高"
          description: "磁盘使用率超过85%，当前值: {{ $value }}%"

      - alert: MySQLDown
        expr: mysql_up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "MySQL服务宕机"

      - alert: AppHighLatency
        expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "应用响应延迟过高"
```

### 2.6 环境变量配置

```bash
# .env
# MySQL配置
MYSQL_ROOT_PASSWORD=Root@123456
MYSQL_DATABASE=data_platform
MYSQL_USER=app_user
MYSQL_PASSWORD=App@123456

# RabbitMQ配置
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=Rabbit@123456

# 应用密钥
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 环境
APP_ENV=production
LOG_LEVEL=INFO
```

---

## 第三阶段：应用部署

### 3.1 项目目录结构

```
/data/
├── docker-compose.yml
├── .env
├── mysql/
│   ├── data/
│   └── my.cnf
├── redis/
│   └── data/
├── es/
│   ├── data/
│   └── elasticsearch.yml
├── rabbitmq/
│   └── data/
├── prometheus/
│   ├── prometheus.yml
│   ├── alert_rules.yml
│   └── data/
├── grafana/
│   └── data/
├── nginx/
│   ├── nginx.conf
│   └── html/
├── app/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── main.py
│   ├── config.py
│   ├── models/
│   ├── routers/
│   ├── services/
│   ├── tasks/
│   └── utils/
└── backups/
```

### 3.2 启动服务

```bash
# 1. 创建数据目录并设置权限
sudo mkdir -p /data/{mysql,redis,es,rabbitmq,prometheus,grafana,nginx,backups}
sudo chmod 777 /data/es/data  # ES需要写权限

# 2. 启动基础服务
cd /data
docker-compose up -d mysql redis elasticsearch rabbitmq

# 3. 等待服务就绪（约30秒）
sleep 30

# 4. 检查服务状态
docker-compose ps

# 5. 启动应用服务
docker-compose up -d app celery-worker prometheus grafana nginx

# 6. 查看日志
docker-compose logs -f app
```

### 3.3 验证部署

```bash
# 检查各服务端口
curl http://localhost:8000/health      # FastAPI
curl http://localhost:9200/_cluster/health  # Elasticsearch
curl http://localhost:9090/-/healthy   # Prometheus
curl http://localhost:3000/api/health  # Grafana

# MySQL连接检查
docker exec -it mysql mysql -u root -p -e "SHOW STATUS LIKE 'Threads_connected';"

# Redis连接检查
docker exec -it redis redis-cli INFO memory
```

---

## 第四阶段：监控配置

### 4.1 Grafana 数据源配置

```bash
# 通过API自动配置数据源
curl -X POST http://admin:admin123@localhost:3000/api/datasources \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Prometheus",
    "type": "prometheus",
    "url": "http://prometheus:9090",
    "access": "proxy",
    "isDefault": true
  }'
```

### 4.2 关键监控指标

| 指标 | 告警阈值 | 说明 |
|------|----------|------|
| 内存使用率 | >85% | 8GB总内存 |
| 磁盘使用率 | >85% | 160GB磁盘 |
| MySQL连接数 | >40 | 最大50连接 |
| API响应时间(P95) | >1000ms | 接口延迟 |
| 错误率 | >5% | HTTP 5xx比例 |
| ES堆内存 | >85% | 1GB堆内存 |

### 4.3 日志收集方案

```bash
# 日志轮转配置（/etc/logrotate.d/data-platform）
/data/logs/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 644 root root
    dateext
    dateformat -%Y%m%d
}
```

---

## 第五阶段：运维管理

### 5.1 数据备份策略

```bash
#!/bin/bash
# backup.sh - 每日凌晨2点执行
BACKUP_DIR="/data/backups"
DATE=$(date +%Y%m%d)
RETAIN_DAYS=7

# MySQL备份
docker exec mysql mysqldump -u root -p"${MYSQL_ROOT_PASSWORD}" \
    --all-databases --single-transaction \
    | gzip > ${BACKUP_DIR}/mysql_${DATE}.sql.gz

# Redis持久化备份
cp /data/redis/data/dump.rdb ${BACKUP_DIR}/redis_${DATE}.rdb

# Elasticsearch快照（简化版 - 使用共享卷）
# 生产环境建议使用S3仓库

# 清理旧备份
find ${BACKUP_DIR} -name "*.gz" -mtime +${RETAIN_DAYS} -delete
find ${BACKUP_DIR} -name "*.rdb" -mtime +${RETAIN_DAYS} -delete

# 磁盘空间检查
DISK_USAGE=$(df /data | awk 'NR==2 {print $5}' | tr -d '%')
if [ $DISK_USAGE -gt 85 ]; then
    echo "WARNING: Disk usage ${DISK_USAGE}%" | mail -s "磁盘告警" admin@example.com
fi
```

### 5.2 数据清理策略

```python
# 数据保留策略（60天）
CLEANUP_SQL = """
-- 删除60天前的数据
DELETE FROM api_logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 60 DAY);
DELETE FROM task_logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 60 DAY);
DELETE FROM operation_logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 60 DAY);

-- 清理ES旧索引（按日期命名）
-- DELETE /logs-$(date -d '60 days ago' +%Y.%m.%d)
"""
```

### 5.3 常用运维命令

```bash
# 查看资源使用
docker stats --no-stream

# 重启单个服务
docker-compose restart app

# 扩展worker数量（临时）
docker-compose up -d --scale celery-worker=2

# 进入容器调试
docker exec -it data-platform-app /bin/sh

# 查看MySQL慢查询
docker exec mysql mysql -u root -p -e "SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 10;"

# Redis清理
docker exec redis redis-cli FLUSHDB  # 谨慎使用

# 强制清理Docker
docker system prune -f
docker volume prune -f
```

---

## 第六阶段：低资源优化策略

### 6.1 内存优化配置

| 服务 | 标准配置 | 低内存模式 | 优化措施 |
|------|----------|------------|----------|
| MySQL | 1GB | 512MB | 减小buffer pool、关闭查询缓存 |
| Elasticsearch | 2GB | 1GB | 堆内存减半、减少分片 |
| FastAPI | 1GB | 512MB | 减少worker数、启用流式响应 |
| Redis | 512MB | 256MB | 减少maxmemory、调整策略 |
| RabbitMQ | 512MB | 256MB | 减少连接限制 |
| Prometheus+Grafana | 512MB | 256MB | 减少保留时间 |

### 6.2 MySQL 低内存配置

```ini
# mysql/my-low-memory.cnf - 适用于总内存<=4GB
[mysqld]
innodb_buffer_pool_size = 256M
innodb_log_buffer_size = 8M
innodb_log_file_size = 64M
key_buffer_size = 16M
query_cache_type = 0
query_cache_size = 0
tmp_table_size = 16M
max_heap_table_size = 16M
max_connections = 20
innodb_flush_log_at_trx_commit = 2
performance_schema = off
```

### 6.3 Elasticsearch 低资源配置

```yaml
# es/low-resource.yml
cluster.name: data-platform
node.name: es-single
discovery.type: single-node

# 堆内存减半
ES_JAVA_OPTS: "-Xms512m -Xmx512m"

# 禁用不必要功能
xpack.security.enabled: false
xpack.monitoring.enabled: false
xpack.watcher.enabled: false

# 减少分片和副本
index.number_of_shards: 1
index.number_of_replicas: 0
index.refresh_interval: 30s

# 内存优化
indices.memory.index_buffer_size = 128mb
indices.queries.cache.size = 64mb
indices.fielddata.cache.size = 64mb
```

### 6.4 FastAPI 低资源配置

```python
# gunicorn低资源配置
# gunicorn.conf.py
bind = "0.0.0.0:8000"
workers = 2  # 减少worker数
worker_class = "uvicorn.workers.UvicornWorker"
worker_connections = 50
max_requests = 500
max_requests_jitter = 50
timeout = 60
keepalive = 5

# 启用流式响应处理大数据
def handle_large_data():
    """使用生成器流式返回，避免内存占用"""
    for chunk in large_dataset:
        yield chunk
```

### 6.5 Celery 低资源配置

```python
# celery低资源配置
celery -A app.tasks worker \
    --loglevel=info \
    --concurrency=2 \          # 减少并发数
    --max-tasks-per-child=50 \ # 防止内存泄漏
    --prefetch-multiplier=1     # 减少预取
```

### 6.6 磁盘空间管理

```bash
#!/bin/bash
# disk-cleanup.sh - 磁盘空间管理脚本

# 1. 清理Docker日志（限制单个容器日志100MB）
find /var/lib/docker/containers -name "*.log" -size +100M -exec sh -c 'cat /dev/null > {}' \;

# 2. 清理旧的数据备份（保留最近7天）
find /data/backups -mtime +7 -type f -delete

# 3. 清理ES旧索引（按日期命名）
CUTOFF_DATE=$(date -d '60 days ago' +%Y.%m.%d)
curl -X DELETE "localhost:9200/*-${CUTOFF_DATE}"

# 4. 清理应用日志
docker exec app find /app/logs -name "*.log" -mtime +14 -delete

# 5. 磁盘告警
DISK_USAGE=$(df /data | awk 'NR==2 {print $5}' | tr -d '%')
if [ $DISK_USAGE -gt 85 ]; then
    echo "磁盘使用率: ${DISK_USAGE}%" | mail -s "【紧急】磁盘空间不足" admin@example.com
fi
```

### 6.7 160GB 磁盘分配建议

```
/               20GB   系统根分区
/data          120GB   应用数据（MySQL、ES、Redis等）
/var/log        10GB   系统日志
swap             8GB   交换分区（内存不足时使用）
/home            2GB   用户目录
```

### 6.8 性能监控脚本

```bash
#!/bin/bash
# monitor.sh - 资源监控脚本

LOG_FILE="/data/logs/system-monitor.log"
mkdir -p $(dirname $LOG_FILE)

echo "=== $(date) ===" >> $LOG_FILE

# 内存使用
echo "内存使用:" >> $LOG_FILE
free -h >> $LOG_FILE

# 磁盘使用
echo -e "\n磁盘使用:" >> $LOG_FILE
df -h /data >> $LOG_FILE

# Docker容器状态
echo -e "\n容器资源使用:" >> $LOG_FILE
docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.NetIO}}" >> $LOG_FILE

# MySQL连接数
echo -e "\nMySQL连接数:" >> $LOG_FILE
docker exec mysql mysql -u root -p"${MYSQL_ROOT_PASSWORD}" -e "SHOW STATUS LIKE 'Threads_connected';" 2>/dev/null >> $LOG_FILE

# ES健康状态
echo -e "\nES健康状态:" >> $LOG_FILE
curl -s http://localhost:9200/_cluster/health | jq -r '.status' >> $LOG_FILE

echo -e "\n" >> $LOG_FILE
```

---

## 附录

### A. 端口映射表

| 服务 | 内部端口 | 外部端口 | 说明 |
|------|----------|----------|------|
| Nginx | 80 | 80 | Web入口 |
| FastAPI | 8000 | 8000 | API服务 |
| MySQL | 3306 | 3306 | 数据库 |
| Redis | 6379 | 6379 | 缓存 |
| Elasticsearch | 9200 | 9200 | 搜索引擎 |
| RabbitMQ | 5672/15672 | 5672/15672 | 消息队列/管理界面 |
| Prometheus | 9090 | 9090 | 监控 |
| Grafana | 3000 | 3000 | 可视化 |

### B. 故障排查

```bash
# 1. 服务无法启动 - 检查日志
docker-compose logs --tail=100 SERVICE_NAME

# 2. 内存不足 - 查看OOM
dmesg | grep -i "out of memory"

# 3. MySQL连接满 - 查看进程列表
SHOW PROCESSLIST;

# 4. ES索引损坏 - 重建索引
curl -X POST "localhost:9200/_cluster/reroute?retry_failed"

# 5. 磁盘满 - 清理空间
du -sh /data/* | sort -rh | head -20
```

### C. 安全配置建议

1. **修改默认端口**：SSH、MySQL等使用非默认端口
2. **启用防火墙**：仅开放必要端口（80, 443, 2222等）
3. **定期更新**：`apt update && apt upgrade`
4. **SSL证书**：生产环境使用HTTPS
5. **访问控制**：Grafana、RabbitMQ管理界面配置强密码
