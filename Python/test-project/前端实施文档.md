# 数据处理平台 - 前端实施文档

## 技术栈

- **框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **UI 组件库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP 客户端**: Axios

---

## 第一阶段：项目初始化

### 步骤 1.1：创建 Vue 3 项目

```bash
# 进入项目根目录
cd /data-platform

# 使用 npm 创建 Vue 3 + TypeScript 项目
npm create vue@latest frontend

# 安装时选择以下选项：
# ✔ Project name: frontend
# ✔ Add TypeScript? Yes
# ✔ Add JSX Support? No
# ✔ Add Vue Router? Yes
# ✔ Add Pinia? Yes
# ✔ Add Vitest? No
# ✔ Add Cypress? No
# ✔ Add ESLint? Yes
# ✔ Add Prettier? Yes
```

### 步骤 1.2：安装依赖

```bash
cd /data-platform/frontend

# 安装 UI 组件库和工具
npm install element-plus axios @element-plus/icons-vue

# 安装开发依赖
npm install -D sass

# 验证安装
npm list
```

### 步骤 1.3：配置开发服务器代理

```bash
cat > /data-platform/frontend/vite.config.ts << 'EOF'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      // 代理 API 请求到后端
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  },
  build: {
    outDir: '../nginx/html',
    emptyOutDir: true
  }
})
EOF
```

---

## 第二阶段：目录结构

### 步骤 2.1：创建完整目录结构

```bash
cd /data-platform/frontend/src

# 创建目录
mkdir -p api/{auth,user,datasource,collect,process,search,analytics}
mkdir -p components/{common,layout,form}
mkdir -p views/{auth,dashboard,user,datasource,collect,process,search,analytics}
mkdir -p stores/modules
mkdir -p router
mkdir -p utils
mkdir -p types
mkdir -p assets/{images,styles}

# 目录说明
tree -L 3 /data-platform/frontend/src
```

**预期结构：**
```
src/
├── api/                    # API 接口封装
│   ├── auth/              # 认证相关
│   ├── user/              # 用户管理
│   ├── datasource/        # 数据源
│   ├── collect/           # 采集任务
│   ├── process/           # 数据处理
│   ├── search/            # 全文检索
│   └── analytics/         # 数据分析
├── assets/                # 静态资源
├── components/            # 组件
│   ├── common/           # 通用组件
│   ├── layout/           # 布局组件
│   └── form/             # 表单组件
├── router/                # 路由配置
├── stores/                # Pinia 状态管理
├── types/                 # TypeScript 类型定义
├── utils/                 # 工具函数
└── views/                 # 页面视图
```

---

## 第三阶段：类型定义

### 步骤 3.1：创建全局类型定义

```bash
cat > /data-platform/frontend/src/types/index.ts << 'EOF'
// ============================================
// 通用类型定义
// ============================================

// API 响应格式
export interface ApiResponse<T = any> {
  code: number
  message: string
  data: T
}

// 分页参数
export interface PaginationParams {
  page: number
  page_size: number
}

// 分页响应
export interface PaginationData<T> {
  total: number
  page: number
  page_size: number
  items: T[]
}

// ============================================
// 用户相关类型
// ============================================

export interface User {
  id: number
  username: string
  nickname?: string
  email?: string
  phone?: string
  avatar?: string
  status: number
  last_login_time?: string
  created_at: string
}

export interface LoginForm {
  username: string
  password: string
}

export interface LoginResult {
  access_token: string
  token_type: string
  expires_in: number
  user: User
}

// ============================================
// 数据源类型
// ============================================

export interface Datasource {
  id: number
  name: string
  type: 'mysql' | 'postgresql' | 'api'
  description?: string
  host?: string
  port?: number
  database_name?: string
  username?: string
  password?: string
  status: number
  last_test_time?: string
  last_test_result?: boolean
  created_at: string
}

export interface DatasourceForm {
  name: string
  type: string
  description?: string
  host?: string
  port?: number
  database_name?: string
  username?: string
  password?: string
}

// ============================================
// 采集任务类型
// ============================================

export interface CollectTask {
  id: number
  name: string
  datasource_id: number
  task_type: 'sql' | 'api' | 'file'
  description?: string
  source_config: Record<string, any>
  target_table: string
  field_mapping?: Record<string, string>
  schedule_type: string
  status: number
  last_run_time?: string
  last_run_status?: string
  created_at: string
}

export interface CollectInstance {
  id: number
  task_id: number
  instance_no: string
  sync_mode: string
  status: string
  progress: number
  total_count: number
  success_count: number
  fail_count: number
  start_at?: string
  end_at?: string
  duration_ms?: number
  error_msg?: string
}

// ============================================
// 数据处理类型
// ============================================

export interface CleanRule {
  rule: 'trim' | 'email_format' | 'range'
  field: string
  action: 'fix' | 'remove_invalid'
  min?: number
  max?: number
}

export interface CleanResult {
  original_count: number
  output_count: number
}

// ============================================
// 搜索类型
// ============================================

export interface SearchResult {
  total: number
  page: number
  page_size: number
  took_ms: number
  items: SearchItem[]
}

export interface SearchItem {
  _id: string
  _score: number
  _source: Record<string, any>
  highlight?: Record<string, string[]>
}

// ============================================
// 分析类型
// ============================================

export interface QueryResult {
  columns: string[]
  rows: Record<string, any>[]
  total: number
  execution_time_ms: number
}
EOF
```

---

## 第四阶段：工具函数

### 步骤 4.1：创建请求封装

```bash
cat > /data-platform/frontend/src/utils/request.ts << 'EOF'
import axios, { AxiosInstance, AxiosError, InternalAxiosRequestConfig } from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/modules/user'

// 创建 axios 实例
const request: AxiosInstance = axios.create({
  baseURL: '/api/v1',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    return config
  },
  (error: AxiosError) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const { data } = response
    
    // 业务错误处理
    if (data.code !== 200) {
      ElMessage.error(data.message || '请求失败')
      return Promise.reject(new Error(data.message))
    }
    
    return data.data
  },
  (error: AxiosError) => {
    const { response } = error
    
    if (response) {
      switch (response.status) {
        case 401:
          ElMessage.error('登录已过期，请重新登录')
          // 清除登录状态并跳转到登录页
          const userStore = useUserStore()
          userStore.logout()
          window.location.href = '/login'
          break
        case 403:
          ElMessage.error('没有权限执行此操作')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 500:
          ElMessage.error('服务器内部错误')
          break
        default:
          ElMessage.error(response.data?.message || '网络错误')
      }
    } else {
      ElMessage.error('网络连接失败')
    }
    
    return Promise.reject(error)
  }
)

export default request
EOF
```

### 步骤 4.2：创建通用工具函数

```bash
cat > /data-platform/frontend/src/utils/index.ts << 'EOF'
// 格式化日期时间
export function formatDateTime(date: string | Date | undefined): string {
  if (!date) return '-'
  const d = new Date(date)
  return d.toLocaleString('zh-CN', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit'
  })
}

// 格式化日期
export function formatDate(date: string | Date | undefined): string {
  if (!date) return '-'
  const d = new Date(date)
  return d.toLocaleDateString('zh-CN')
}

// 状态标签映射
export const statusMap: Record<number, { text: string; type: 'success' | 'info' | 'warning' | 'danger' }> = {
  0: { text: '禁用', type: 'danger' },
  1: { text: '启用', type: 'success' }
}

// 采集任务状态映射
export const collectStatusMap: Record<string, { text: string; type: 'success' | 'info' | 'warning' | 'danger' }> = {
  pending: { text: '等待中', type: 'info' },
  running: { text: '运行中', type: 'warning' },
  completed: { text: '已完成', type: 'success' },
  failed: { text: '失败', type: 'danger' }
}

// 数据源类型映射
export const datasourceTypeMap: Record<string, string> = {
  mysql: 'MySQL',
  postgresql: 'PostgreSQL',
  api: 'API接口'
}

// 下载文件
export function downloadFile(content: Blob, filename: string) {
  const url = window.URL.createObjectURL(content)
  const link = document.createElement('a')
  link.href = url
  link.download = filename
  document.body.appendChild(link)
  link.click()
  document.body.removeChild(link)
  window.URL.revokeObjectURL(url)
}
EOF
```

---

## 第五阶段：状态管理

### 步骤 5.1：创建用户状态管理

```bash
cat > /data-platform/frontend/src/stores/modules/user.ts << 'EOF'
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { User, LoginForm } from '@/types'
import { login, getUserInfo } from '@/api/auth'

export const useUserStore = defineStore('user', () => {
  // State
  const token = ref<string>(localStorage.getItem('token') || '')
  const userInfo = ref<User | null>(null)

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const username = computed(() => userInfo.value?.username || '')

  // Actions
  async function loginAction(form: LoginForm) {
    const result = await login(form)
    token.value = result.access_token
    userInfo.value = result.user
    localStorage.setItem('token', result.access_token)
    return result
  }

  async function fetchUserInfo() {
    if (!token.value) return
    try {
      const data = await getUserInfo()
      userInfo.value = data
    } catch {
      logout()
    }
  }

  function logout() {
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }

  return {
    token,
    userInfo,
    isLoggedIn,
    username,
    loginAction,
    fetchUserInfo,
    logout
  }
})
EOF
```

### 步骤 5.2：创建应用状态管理

```bash
cat > /data-platform/frontend/src/stores/modules/app.ts << 'EOF'
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useAppStore = defineStore('app', () => {
  // State
  const sidebarCollapsed = ref<boolean>(false)
  const loading = ref<boolean>(false)

  // Actions
  function toggleSidebar() {
    sidebarCollapsed.value = !sidebarCollapsed.value
  }

  function setLoading(status: boolean) {
    loading.value = status
  }

  return {
    sidebarCollapsed,
    loading,
    toggleSidebar,
    setLoading
  }
})
EOF
```

### 步骤 5.3：创建 Store 入口

```bash
cat > /data-platform/frontend/src/stores/index.ts << 'EOF'
import { createPinia } from 'pinia'

export const pinia = createPinia()

export * from './modules/user'
export * from './modules/app'
EOF
```

---

## 第六阶段：API 接口封装

### 步骤 6.1：创建认证 API

```bash
cat > /data-platform/frontend/src/api/auth/index.ts << 'EOF'
import request from '@/utils/request'
import type { LoginForm, LoginResult, User } from '@/types'

export function login(data: LoginForm): Promise<LoginResult> {
  const params = new URLSearchParams()
  params.append('username', data.username)
  params.append('password', data.password)
  
  return request.post('/auth/login', params, {
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' }
  })
}

export function getUserInfo(): Promise<User> {
  return request.get('/auth/me')
}
EOF
```

### 步骤 6.2：创建用户管理 API

```bash
cat > /data-platform/frontend/src/api/user/index.ts << 'EOF'
import request from '@/utils/request'
import type { User, PaginationData, PaginationParams } from '@/types'

export function getUserList(params: PaginationParams): Promise<PaginationData<User>> {
  return request.get('/users/', { params })
}

export function createUser(data: Partial<User>): Promise<User> {
  return request.post('/users/', data)
}

export function deleteUser(id: number): Promise<void> {
  return request.delete(`/users/${id}`)
}
EOF
```

### 步骤 6.3：创建数据源 API

```bash
cat > /data-platform/frontend/src/api/datasource/index.ts << 'EOF'
import request from '@/utils/request'
import type { Datasource, DatasourceForm, PaginationData, PaginationParams } from '@/types'

export function getDatasourceList(params: PaginationParams & { type?: string }): Promise<PaginationData<Datasource>> {
  return request.get('/datasources/', { params })
}

export function createDatasource(data: DatasourceForm): Promise<Datasource> {
  return request.post('/datasources/', data)
}

export function testConnection(id: number): Promise<{ connected: boolean; error?: string; latency_ms?: number }> {
  return request.post(`/datasources/${id}/test`)
}
EOF
```

### 步骤 6.4：创建采集任务 API

```bash
cat > /data-platform/frontend/src/api/collect/index.ts << 'EOF'
import request from '@/utils/request'
import type { CollectTask, CollectInstance, PaginationData, PaginationParams } from '@/types'

export function getCollectTaskList(params: PaginationParams): Promise<PaginationData<CollectTask>> {
  return request.get('/collect/tasks', { params })
}

export function createCollectTask(data: Partial<CollectTask>): Promise<CollectTask> {
  return request.post('/collect/tasks', data)
}

export function executeTask(
  taskId: number, 
  data: { sync_mode?: string; start_time?: string; end_time?: string }
): Promise<{ instance_id: number; instance_no: string; status: string }> {
  return request.post(`/collect/tasks/${taskId}/execute`, data)
}
EOF
```

### 步骤 6.5：创建数据处理 API

```bash
cat > /data-platform/frontend/src/api/process/index.ts << 'EOF'
import request from '@/utils/request'
import type { CleanRule, CleanResult, QueryResult } from '@/types'

export function cleanData(
  inputTable: string, 
  outputTable: string, 
  rules: CleanRule[]
): Promise<CleanResult> {
  return request.post('/process/clean', null, {
    params: { input_table: inputTable, output_table: outputTable, rules }
  })
}

export function executeQuery(sql: string, params?: Record<string, any>): Promise<QueryResult> {
  return request.post('/analytics/query', { sql, params })
}
EOF
```

### 步骤 6.6：创建搜索 API

```bash
cat > /data-platform/frontend/src/api/search/index.ts << 'EOF'
import request from '@/utils/request'
import type { SearchResult } from '@/types'

export function search(
  query: string, 
  index: string, 
  fields: string[], 
  page: number = 1, 
  pageSize: number = 20
): Promise<SearchResult> {
  return request.post('/search/', null, {
    params: { query, index, fields, page, page_size: pageSize }
  })
}
EOF
```

---

## 第七阶段：路由配置

### 步骤 7.1：创建路由配置

```bash
cat > /data-platform/frontend/src/router/index.ts << 'EOF'
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/stores'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/auth/Login.vue'),
      meta: { public: true }
    },
    {
      path: '/',
      name: 'Layout',
      component: () => import('@/components/layout/MainLayout.vue'),
      redirect: '/dashboard',
      children: [
        {
          path: '/dashboard',
          name: 'Dashboard',
          component: () => import('@/views/dashboard/Index.vue'),
          meta: { title: '数据概览', icon: 'Odometer' }
        },
        {
          path: '/datasource',
          name: 'Datasource',
          component: () => import('@/views/datasource/Index.vue'),
          meta: { title: '数据源管理', icon: 'Connection' }
        },
        {
          path: '/collect',
          name: 'Collect',
          component: () => import('@/views/collect/Index.vue'),
          meta: { title: '采集任务', icon: 'Download' }
        },
        {
          path: '/process',
          name: 'Process',
          component: () => import('@/views/process/Index.vue'),
          meta: { title: '数据处理', icon: 'MagicStick' }
        },
        {
          path: '/search',
          name: 'Search',
          component: () => import('@/views/search/Index.vue'),
          meta: { title: '全文检索', icon: 'Search' }
        },
        {
          path: '/analytics',
          name: 'Analytics',
          component: () => import('@/views/analytics/Index.vue'),
          meta: { title: '数据分析', icon: 'TrendCharts' }
        },
        {
          path: '/user',
          name: 'User',
          component: () => import('@/views/user/Index.vue'),
          meta: { title: '用户管理', icon: 'User' }
        }
      ]
    },
    {
      path: '/:pathMatch(.*)*',
      name: 'NotFound',
      component: () => import('@/views/NotFound.vue')
    }
  ]
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  
  if (to.meta.public) {
    // 公开页面，直接访问
    next()
  } else if (!userStore.isLoggedIn) {
    // 未登录，跳转到登录页
    next('/login')
  } else {
    // 已登录，允许访问
    next()
  }
})

export default router
EOF
```

---

## 第八阶段：布局组件

### 步骤 8.1：创建主布局组件

```bash
cat > /data-platform/frontend/src/components/layout/MainLayout.vue << 'EOF'
<template>
  <el-container class="main-layout">
    <!-- 侧边栏 -->
    <el-aside :width="sidebarWidth" class="sidebar">
      <div class="logo">
        <span v-show="!appStore.sidebarCollapsed">数据处理平台</span>
      </div>
      <el-menu
        :default-active="$route.path"
        :collapse="appStore.sidebarCollapsed"
        :collapse-transition="false"
        router
        background-color="#304156"
        text-color="#bfcbd9"
        active-text-color="#409EFF"
      >
        <el-menu-item v-for="route in menuRoutes" :key="route.path" :index="route.path">
          <el-icon>
            <component :is="route.meta?.icon" />
          </el-icon>
          <template #title>{{ route.meta?.title }}</template>
        </el-menu-item>
      </el-menu>
    </el-aside>

    <el-container>
      <!-- 顶部导航 -->
      <el-header class="header">
        <div class="header-left">
          <el-icon class="collapse-btn" @click="appStore.toggleSidebar">
            <Fold v-if="!appStore.sidebarCollapsed" />
            <Expand v-else />
          </el-icon>
          <breadcrumb />
        </div>
        <div class="header-right">
          <el-dropdown @command="handleCommand">
            <span class="user-info">
              {{ userStore.username }}
              <el-icon><ArrowDown /></el-icon>
            </span>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item command="logout">退出登录</el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>

      <!-- 主内容区 -->
      <el-main class="main-content">
        <router-view v-slot="{ Component }">
          <transition name="fade" mode="out-in">
            <component :is="Component" />
          </transition>
        </router-view>
      </el-main>
    </el-container>
  </el-container>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore, useAppStore } from '@/stores'
import { ElMessageBox } from 'element-plus'
import Breadcrumb from './Breadcrumb.vue'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()
const appStore = useAppStore()

const sidebarWidth = computed(() => appStore.sidebarCollapsed ? '64px' : '210px')

const menuRoutes = computed(() => {
  const layoutRoute = route.matched.find(r => r.name === 'Layout')
  return layoutRoute?.children?.filter(r => r.meta?.title) || []
})

function handleCommand(command: string) {
  if (command === 'logout') {
    ElMessageBox.confirm('确定要退出登录吗？', '提示', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      type: 'warning'
    }).then(() => {
      userStore.logout()
      router.push('/login')
    })
  }
}
</script>

<style scoped lang="scss">
.main-layout {
  height: 100vh;
}

.sidebar {
  background-color: #304156;
  transition: width 0.3s;

  .logo {
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    font-size: 18px;
    font-weight: bold;
    border-bottom: 1px solid #1f2d3d;
  }

  .el-menu {
    border-right: none;
  }
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background-color: #fff;
  box-shadow: 0 1px 4px rgba(0, 21, 41, 0.08);

  .header-left {
    display: flex;
    align-items: center;

    .collapse-btn {
      font-size: 20px;
      cursor: pointer;
      margin-right: 15px;

      &:hover {
        color: #409EFF;
      }
    }
  }

  .header-right {
    .user-info {
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 5px;
    }
  }
}

.main-content {
  background-color: #f0f2f5;
  padding: 20px;
  overflow-y: auto;
}

.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.3s ease;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
</style>
EOF
```

### 步骤 8.2：创建面包屑组件

```bash
cat > /data-platform/frontend/src/components/layout/Breadcrumb.vue << 'EOF'
<template>
  <el-breadcrumb separator="/">
    <el-breadcrumb-item :to="{ path: '/' }">首页</el-breadcrumb-item>
    <el-breadcrumb-item v-if="currentRoute.meta?.title">
      {{ currentRoute.meta.title }}
    </el-breadcrumb-item>
  </el-breadcrumb>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'

const route = useRoute()
const currentRoute = computed(() => route)
</script>
EOF
```

---

## 第九阶段：页面实现

### 步骤 9.1：创建登录页面

```bash
cat > /data-platform/frontend/src/views/auth/Login.vue << 'EOF'
<template>
  <div class="login-page">
    <el-card class="login-box">
      <h2 class="title">数据处理平台</h2>
      <el-form
        ref="formRef"
        :model="form"
        :rules="rules"
        @keyup.enter="handleLogin"
      >
        <el-form-item prop="username">
          <el-input
            v-model="form.username"
            placeholder="用户名"
            :prefix-icon="User"
            size="large"
          />
        </el-form-item>
        <el-form-item prop="password">
          <el-input
            v-model="form.password"
            type="password"
            placeholder="密码"
            :prefix-icon="Lock"
            size="large"
            show-password
          />
        </el-form-item>
        <el-form-item>
          <el-button
            type="primary"
            size="large"
            :loading="loading"
            @click="handleLogin"
            style="width: 100%"
          >
            登录
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { User, Lock } from '@element-plus/icons-vue'
import { ElMessage } from 'element-plus'
import type { FormInstance, FormRules } from 'element-plus'
import { useUserStore } from '@/stores'

const router = useRouter()
const userStore = useUserStore()
const formRef = ref<FormInstance>()
const loading = ref(false)

const form = reactive({
  username: '',
  password: ''
})

const rules: FormRules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}

async function handleLogin() {
  if (!formRef.value) return
  
  await formRef.value.validate(async (valid) => {
    if (valid) {
      loading.value = true
      try {
        await userStore.loginAction(form)
        ElMessage.success('登录成功')
        router.push('/')
      } catch (error) {
        // 错误已在请求拦截器处理
      } finally {
        loading.value = false
      }
    }
  })
}
</script>

<style scoped lang="scss">
.login-page {
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

  .login-box {
    width: 400px;
    padding: 20px;

    .title {
      text-align: center;
      margin-bottom: 30px;
      color: #333;
    }
  }
}
</style>
EOF
```

### 步骤 9.2：创建数据概览页面

```bash
cat > /data-platform/frontend/src/views/dashboard/Index.vue << 'EOF'
<template>
  <div class="dashboard">
    <el-row :gutter="20">
      <el-col :span="6" v-for="stat in stats" :key="stat.title">
        <el-card shadow="hover">
          <div class="stat-card">
            <div class="stat-icon" :style="{ backgroundColor: stat.color }">
              <el-icon :size="24" color="#fff">
                <component :is="stat.icon" />
              </el-icon>
            </div>
            <div class="stat-info">
              <div class="stat-value">{{ stat.value }}</div>
              <div class="stat-title">{{ stat.title }}</div>
            </div>
          </div>
        </el-card>
      </el-col>
    </el-row>

    <el-row :gutter="20" style="margin-top: 20px">
      <el-col :span="12">
        <el-card>
          <template #header>采集任务状态分布</template>
          <div class="chart-placeholder">
            <el-empty description="图表组件待集成" />
          </div>
        </el-card>
      </el-col>
      <el-col :span="12">
        <el-card>
          <template #header>最近采集记录</template>
          <el-table :data="recentTasks" size="small">
            <el-table-column prop="name" label="任务名称" />
            <el-table-column prop="status" label="状态">
              <template #default="{ row }">
                <el-tag :type="getStatusType(row.status)">
                  {{ getStatusText(row.status) }}
                </el-tag>
              </template>
            </el-table-column>
            <el-table-column prop="time" label="时间" />
          </el-table>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { Connection, Download, Document, User } from '@element-plus/icons-vue'

const stats = ref([
  { title: '数据源', value: 12, icon: Connection, color: '#409EFF' },
  { title: '采集任务', value: 28, icon: Download, color: '#67C23A' },
  { title: '数据表', value: 156, icon: Document, color: '#E6A23C' },
  { title: '用户', value: 8, icon: User, color: '#F56C6C' }
])

const recentTasks = ref([
  { name: '用户数据同步', status: 'completed', time: '2024-01-15 10:30' },
  { name: '订单数据导入', status: 'running', time: '2024-01-15 09:15' },
  { name: '日志数据清洗', status: 'failed', time: '2024-01-15 08:00' }
])

function getStatusType(status: string) {
  const map: Record<string, string> = {
    completed: 'success',
    running: 'warning',
    failed: 'danger'
  }
  return map[status] || 'info'
}

function getStatusText(status: string) {
  const map: Record<string, string> = {
    completed: '完成',
    running: '运行中',
    failed: '失败'
  }
  return map[status] || status
}
</script>

<style scoped lang="scss">
.stat-card {
  display: flex;
  align-items: center;

  .stat-icon {
    width: 60px;
    height: 60px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 15px;
  }

  .stat-info {
    .stat-value {
      font-size: 24px;
      font-weight: bold;
      color: #333;
    }

    .stat-title {
      font-size: 14px;
      color: #999;
      margin-top: 5px;
    }
  }
}

.chart-placeholder {
  height: 300px;
  display: flex;
  align-items: center;
  justify-content: center;
}
</style>
EOF
```

### 步骤 9.3：创建数据源管理页面

```bash
cat > /data-platform/frontend/src/views/datasource/Index.vue << 'EOF'
<template>
  <div class="datasource-page">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>数据源管理</span>
          <el-button type="primary" @click="handleAdd">
            <el-icon><Plus /></el-icon>新增数据源
          </el-button>
        </div>
      </template>

      <el-table :data="tableData" v-loading="loading" stripe>
        <el-table-column prop="name" label="名称" />
        <el-table-column prop="type" label="类型">
          <template #default="{ row }">
            {{ typeMap[row.type] }}
          </template>
        </el-table-column>
        <el-table-column prop="host" label="主机" />
        <el-table-column prop="database_name" label="数据库" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <el-tag :type="row.status === 1 ? 'success' : 'danger'">
              {{ row.status === 1 ? '启用' : '禁用' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column label="操作" width="200" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" @click="handleTest(row)">测试</el-button>
            <el-button link type="primary" @click="handleEdit(row)">编辑</el-button>
            <el-button link type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <div class="pagination">
        <el-pagination
          v-model:current-page="query.page"
          v-model:page-size="query.page_size"
          :total="total"
          layout="total, sizes, prev, pager, next"
          @change="fetchData"
        />
      </div>
    </el-card>

    <!-- 新增/编辑弹窗 -->
    <el-dialog v-model="dialogVisible" :title="dialogTitle" width="600px">
      <el-form ref="formRef" :model="form" :rules="rules" label-width="100px">
        <el-form-item label="名称" prop="name">
          <el-input v-model="form.name" />
        </el-form-item>
        <el-form-item label="类型" prop="type">
          <el-select v-model="form.type" style="width: 100%">
            <el-option label="MySQL" value="mysql" />
            <el-option label="PostgreSQL" value="postgresql" />
            <el-option label="API接口" value="api" />
          </el-select>
        </el-form-item>
        <el-form-item label="主机" prop="host">
          <el-input v-model="form.host" />
        </el-form-item>
        <el-form-item label="端口" prop="port">
          <el-input-number v-model="form.port" :min="1" :max="65535" style="width: 100%" />
        </el-form-item>
        <el-form-item label="数据库" prop="database_name">
          <el-input v-model="form.database_name" />
        </el-form-item>
        <el-form-item label="用户名" prop="username">
          <el-input v-model="form.username" />
        </el-form-item>
        <el-form-item label="密码" prop="password">
          <el-input v-model="form.password" type="password" show-password />
        </el-form-item>
        <el-form-item label="描述" prop="description">
          <el-input v-model="form.description" type="textarea" :rows="3" />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button type="primary" @click="handleSubmit" :loading="submitLoading">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { Plus } from '@element-plus/icons-vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import type { FormInstance, FormRules } from 'element-plus'
import { getDatasourceList, createDatasource, testConnection } from '@/api/datasource'
import type { Datasource, DatasourceForm } from '@/types'

const loading = ref(false)
const tableData = ref<Datasource[]>([])
const total = ref(0)
const query = reactive({
  page: 1,
  page_size: 20
})

const typeMap: Record<string, string> = {
  mysql: 'MySQL',
  postgresql: 'PostgreSQL',
  api: 'API接口'
}

// 弹窗相关
const dialogVisible = ref(false)
const dialogTitle = ref('新增数据源')
const submitLoading = ref(false)
const formRef = ref<FormInstance>()
const form = reactive<DatasourceForm>({
  name: '',
  type: 'mysql',
  host: '',
  port: 3306,
  database_name: '',
  username: '',
  password: '',
  description: ''
})

const rules: FormRules = {
  name: [{ required: true, message: '请输入名称', trigger: 'blur' }],
  type: [{ required: true, message: '请选择类型', trigger: 'change' }],
  host: [{ required: true, message: '请输入主机地址', trigger: 'blur' }]
}

async function fetchData() {
  loading.value = true
  try {
    const res = await getDatasourceList(query)
    tableData.value = res.items
    total.value = res.total
  } finally {
    loading.value = false
  }
}

function handleAdd() {
  dialogTitle.value = '新增数据源'
  dialogVisible.value = true
  // 重置表单
  Object.assign(form, {
    name: '',
    type: 'mysql',
    host: '',
    port: 3306,
    database_name: '',
    username: '',
    password: '',
    description: ''
  })
}

function handleEdit(row: Datasource) {
  dialogTitle.value = '编辑数据源'
  dialogVisible.value = true
  Object.assign(form, row)
}

async function handleDelete(row: Datasource) {
  try {
    await ElMessageBox.confirm('确定要删除该数据源吗？', '提示', { type: 'warning' })
    // await deleteDatasource(row.id)
    ElMessage.success('删除成功')
    fetchData()
  } catch {
    // 取消删除
  }
}

async function handleTest(row: Datasource) {
  const loading = ElMessage.info({ message: '连接测试中...', duration: 0 })
  try {
    const res = await testConnection(row.id)
    loading.close()
    if (res.connected) {
      ElMessage.success(`连接成功，延迟: ${res.latency_ms?.toFixed(2)}ms`)
    } else {
      ElMessage.error(`连接失败: ${res.error}`)
    }
  } catch {
    loading.close()
  }
}

async function handleSubmit() {
  if (!formRef.value) return
  await formRef.value.validate(async (valid) => {
    if (valid) {
      submitLoading.value = true
      try {
        await createDatasource(form)
        ElMessage.success('创建成功')
        dialogVisible.value = false
        fetchData()
      } finally {
        submitLoading.value = false
      }
    }
  })
}

onMounted(fetchData)
</script>

<style scoped lang="scss">
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.pagination {
  margin-top: 20px;
  display: flex;
  justify-content: flex-end;
}
</style>
EOF
```

### 步骤 9.4：创建采集任务页面

```bash
cat > /data-platform/frontend/src/views/collect/Index.vue << 'EOF'
<template>
  <div class="collect-page">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>采集任务管理</span>
          <el-button type="primary" @click="handleAdd">
            <el-icon><Plus /></el-icon>新建任务
          </el-button>
        </div>
      </template>

      <el-table :data="tableData" v-loading="loading" stripe>
        <el-table-column prop="name" label="任务名称" />
        <el-table-column prop="task_type" label="类型">
          <template #default="{ row }">
            <el-tag>{{ row.task_type }}</el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="target_table" label="目标表" />
        <el-table-column prop="schedule_type" label="调度方式" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <el-tag :type="row.status === 1 ? 'success' : 'danger'">
              {{ row.status === 1 ? '启用' : '禁用' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="last_run_status" label="上次运行">
          <template #default="{ row }">
            <el-tag v-if="row.last_run_status" :type="getRunStatusType(row.last_run_status)">
              {{ getRunStatusText(row.last_run_status) }}
            </el-tag>
            <span v-else>-</span>
          </template>
        </el-table-column>
        <el-table-column label="操作" width="200" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" @click="handleExecute(row)">执行</el-button>
            <el-button link type="primary" @click="handleEdit(row)">编辑</el-button>
            <el-button link type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <div class="pagination">
        <el-pagination
          v-model:current-page="query.page"
          v-model:page-size="query.page_size"
          :total="total"
          layout="total, sizes, prev, pager, next"
          @change="fetchData"
        />
      </div>
    </el-card>

    <!-- 执行任务弹窗 -->
    <el-dialog v-model="executeDialogVisible" title="执行采集任务" width="500px">
      <el-form :model="executeForm" label-width="100px">
        <el-form-item label="同步模式">
          <el-radio-group v-model="executeForm.sync_mode">
            <el-radio label="full">全量同步</el-radio>
            <el-radio label="incremental">增量同步</el-radio>
          </el-radio-group>
        </el-form-item>
        <el-form-item label="时间范围" v-if="executeForm.sync_mode === 'incremental'">
          <el-date-picker
            v-model="executeForm.timeRange"
            type="datetimerange"
            range-separator="至"
            start-placeholder="开始时间"
            end-placeholder="结束时间"
          />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="executeDialogVisible = false">取消</el-button>
        <el-button type="primary" @click="submitExecute" :loading="executeLoading">执行</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { Plus } from '@element-plus/icons-vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { getCollectTaskList, executeTask } from '@/api/collect'
import type { CollectTask } from '@/types'

const loading = ref(false)
const tableData = ref<CollectTask[]>([])
const total = ref(0)
const query = reactive({
  page: 1,
  page_size: 20
})

const executeDialogVisible = ref(false)
const executeLoading = ref(false)
const currentTask = ref<CollectTask | null>(null)
const executeForm = reactive({
  sync_mode: 'incremental',
  timeRange: [] as Date[]
})

function getRunStatusType(status: string) {
  const map: Record<string, string> = {
    completed: 'success',
    running: 'warning',
    failed: 'danger',
    pending: 'info'
  }
  return map[status] || 'info'
}

function getRunStatusText(status: string) {
  const map: Record<string, string> = {
    completed: '成功',
    running: '运行中',
    failed: '失败',
    pending: '等待中'
  }
  return map[status] || status
}

async function fetchData() {
  loading.value = true
  try {
    const res = await getCollectTaskList(query)
    tableData.value = res.items
    total.value = res.total
  } finally {
    loading.value = false
  }
}

function handleAdd() {
  // 跳转到任务创建页面或打开弹窗
  ElMessage.info('功能开发中...')
}

function handleEdit(row: CollectTask) {
  ElMessage.info('功能开发中...')
}

async function handleDelete(row: CollectTask) {
  try {
    await ElMessageBox.confirm('确定要删除该任务吗？', '提示', { type: 'warning' })
    ElMessage.success('删除成功')
    fetchData()
  } catch {
    // 取消
  }
}

function handleExecute(row: CollectTask) {
  currentTask.value = row
  executeDialogVisible.value = true
  executeForm.sync_mode = 'incremental'
  executeForm.timeRange = []
}

async function submitExecute() {
  if (!currentTask.value) return
  
  executeLoading.value = true
  try {
    await executeTask(currentTask.value.id, {
      sync_mode: executeForm.sync_mode,
      start_time: executeForm.timeRange[0]?.toISOString(),
      end_time: executeForm.timeRange[1]?.toISOString()
    })
    ElMessage.success('任务已提交执行')
    executeDialogVisible.value = false
    fetchData()
  } finally {
    executeLoading.value = false
  }
}

onMounted(fetchData)
</script>

<style scoped lang="scss">
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.pagination {
  margin-top: 20px;
  display: flex;
  justify-content: flex-end;
}
</style>
EOF
```

### 步骤 9.5：创建数据处理页面

```bash
cat > /data-platform/frontend/src/views/process/Index.vue << 'EOF'
<template>
  <div class="process-page">
    <el-row :gutter="20">
      <el-col :span="12">
        <el-card>
          <template #header>数据清洗</template>
          <el-form :model="cleanForm" label-width="100px">
            <el-form-item label="输入表">
              <el-input v-model="cleanForm.input_table" placeholder="请输入源表名" />
            </el-form-item>
            <el-form-item label="输出表">
              <el-input v-model="cleanForm.output_table" placeholder="请输入目标表名" />
            </el-form-item>
            <el-form-item label="清洗规则">
              <div v-for="(rule, index) in cleanForm.rules" :key="index" class="rule-item">
                <el-select v-model="rule.rule" placeholder="规则类型" style="width: 120px">
                  <el-option label="去除空格" value="trim" />
                  <el-option label="邮箱格式" value="email_format" />
                  <el-option label="数值范围" value="range" />
                </el-select>
                <el-input v-model="rule.field" placeholder="字段名" style="width: 150px; margin: 0 10px" />
                <el-select v-model="rule.action" placeholder="处理方式" style="width: 120px">
                  <el-option label="修正" value="fix" />
                  <el-option label="删除无效" value="remove_invalid" />
                </el-select>
                <el-button type="danger" link @click="removeRule(index)">删除</el-button>
              </div>
              <el-button type="primary" link @click="addRule">
                <el-icon><Plus /></el-icon>添加规则
              </el-button>
            </el-form-item>
            <el-form-item>
              <el-button type="primary" @click="handleClean" :loading="cleanLoading">
                开始清洗
              </el-button>
            </el-form-item>
          </el-form>
        </el-card>
      </el-col>

      <el-col :span="12">
        <el-card>
          <template #header>SQL 查询</template>
          <el-input
            v-model="sqlQuery"
            type="textarea"
            :rows="6"
            placeholder="输入 SQL 查询语句"
          />
          <div style="margin-top: 15px">
            <el-button type="primary" @click="executeQuery" :loading="queryLoading">
              执行查询
            </el-button>
          </div>
          
          <div v-if="queryResult" class="query-result">
            <el-divider />
            <div class="result-info">
              共 {{ queryResult.total }} 条记录，执行时间 {{ queryResult.execution_time_ms }}ms
            </div>
            <el-table :data="queryResult.rows" size="small" max-height="300">
              <el-table-column
                v-for="col in queryResult.columns"
                :key="col"
                :prop="col"
                :label="col"
              />
            </el-table>
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { Plus } from '@element-plus/icons-vue'
import { ElMessage } from 'element-plus'
import { cleanData, executeQuery as executeQueryApi } from '@/api/process'
import type { CleanRule, QueryResult } from '@/types'

const cleanForm = reactive({
  input_table: '',
  output_table: '',
  rules: [] as CleanRule[]
})
const cleanLoading = ref(false)

const sqlQuery = ref('SELECT * FROM sys_user LIMIT 10')
const queryLoading = ref(false)
const queryResult = ref<QueryResult | null>(null)

function addRule() {
  cleanForm.rules.push({
    rule: 'trim',
    field: '',
    action: 'fix'
  })
}

function removeRule(index: number) {
  cleanForm.rules.splice(index, 1)
}

async function handleClean() {
  if (!cleanForm.input_table || !cleanForm.output_table) {
    ElMessage.warning('请输入输入表和输出表')
    return
  }
  cleanLoading.value = true
  try {
    const res = await cleanData(cleanForm.input_table, cleanForm.output_table, cleanForm.rules)
    ElMessage.success(`清洗完成：原始 ${res.original_count} 条，输出 ${res.output_count} 条`)
  } finally {
    cleanLoading.value = false
  }
}

async function executeQuery() {
  if (!sqlQuery.value.trim()) {
    ElMessage.warning('请输入 SQL 语句')
    return
  }
  queryLoading.value = true
  try {
    queryResult.value = await executeQueryApi(sqlQuery.value)
  } finally {
    queryLoading.value = false
  }
}
</script>

<style scoped lang="scss">
.rule-item {
  display: flex;
  align-items: center;
  margin-bottom: 10px;
}

.query-result {
  margin-top: 20px;

  .result-info {
    margin-bottom: 10px;
    color: #666;
    font-size: 14px;
  }
}
</style>
EOF
```

### 步骤 9.6：创建全文检索页面

```bash
cat > /data-platform/frontend/src/views/search/Index.vue << 'EOF'
<template>
  <div class="search-page">
    <el-card>
      <template #header>全文检索</template>
      
      <div class="search-box">
        <el-input
          v-model="searchForm.query"
          placeholder="请输入搜索关键词"
          size="large"
          clearable
          @keyup.enter="handleSearch"
        >
          <template #append>
            <el-button type="primary" @click="handleSearch" :loading="loading">
              <el-icon><Search /></el-icon>
            </el-button>
          </template>
        </el-input>
      </div>

      <el-form :inline="true" :model="searchForm" class="search-filter">
        <el-form-item label="索引">
          <el-select v-model="searchForm.index" placeholder="选择索引" style="width: 200px">
            <el-option label="文章索引" value="articles" />
            <el-option label="日志索引" value="logs" />
            <el-option label="产品索引" value="products" />
          </el-select>
        </el-form-item>
        <el-form-item label="搜索字段">
          <el-select
            v-model="searchForm.fields"
            multiple
            placeholder="选择搜索字段"
            style="width: 300px"
          >
            <el-option label="标题" value="title" />
            <el-option label="内容" value="content" />
            <el-option label="作者" value="author" />
            <el-option label="标签" value="tags" />
          </el-select>
        </el-form-item>
      </el-form>

      <div v-if="searchResult" class="search-result">
        <div class="result-summary">
          找到约 {{ searchResult.total }} 条结果（用时 {{ searchResult.took_ms }}ms）
        </div>
        
        <div class="result-list">
          <div
            v-for="item in searchResult.items"
            :key="item._id"
            class="result-item"
          >
            <h4 class="result-title" v-html="getHighlightedTitle(item)"></h4>
            <p class="result-content" v-html="getHighlightedContent(item)"></p>
            <div class="result-meta">
              <span>ID: {{ item._id }}</span>
              <span>相关度: {{ item._score.toFixed(2) }}</span>
            </div>
          </div>
        </div>

        <div class="pagination">
          <el-pagination
            v-model:current-page="searchForm.page"
            v-model:page-size="searchForm.page_size"
            :total="searchResult.total"
            layout="prev, pager, next"
            @change="handleSearch"
          />
        </div>
      </div>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { Search } from '@element-plus/icons-vue'
import { search as searchApi } from '@/api/search'
import type { SearchResult, SearchItem } from '@/types'

const loading = ref(false)
const searchResult = ref<SearchResult | null>(null)

const searchForm = reactive({
  query: '',
  index: 'articles',
  fields: ['title', 'content'],
  page: 1,
  page_size: 20
})

async function handleSearch() {
  if (!searchForm.query.trim()) return
  
  loading.value = true
  try {
    searchResult.value = await searchApi(
      searchForm.query,
      searchForm.index,
      searchForm.fields,
      searchForm.page,
      searchForm.page_size
    )
  } finally {
    loading.value = false
  }
}

function getHighlightedTitle(item: SearchItem): string {
  if (item.highlight?.title) {
    return item.highlight.title[0]
  }
  return item._source.title || '无标题'
}

function getHighlightedContent(item: SearchItem): string {
  if (item.highlight?.content) {
    return item.highlight.content[0]
  }
  const content = item._source.content || ''
  return content.length > 200 ? content.slice(0, 200) + '...' : content
}
</script>

<style scoped lang="scss">
.search-box {
  max-width: 600px;
  margin: 0 auto 20px;
}

.search-filter {
  text-align: center;
  margin-bottom: 20px;
}

.search-result {
  .result-summary {
    color: #666;
    font-size: 14px;
    margin-bottom: 15px;
    padding-bottom: 10px;
    border-bottom: 1px solid #eee;
  }

  .result-list {
    .result-item {
      padding: 15px 0;
      border-bottom: 1px solid #eee;

      .result-title {
        margin: 0 0 10px;
        color: #1a0dab;
        font-size: 18px;

        :deep(mark) {
          background-color: #ffeb3b;
          padding: 0 2px;
        }
      }

      .result-content {
        margin: 0 0 10px;
        color: #333;
        font-size: 14px;
        line-height: 1.6;

        :deep(mark) {
          background-color: #ffeb3b;
          padding: 0 2px;
        }
      }

      .result-meta {
        font-size: 12px;
        color: #999;

        span {
          margin-right: 20px;
        }
      }
    }
  }

  .pagination {
    margin-top: 20px;
    display: flex;
    justify-content: center;
  }
}
</style>
EOF
```

### 步骤 9.7：创建用户管理页面

```bash
cat > /data-platform/frontend/src/views/user/Index.vue << 'EOF'
<template>
  <div class="user-page">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>用户管理</span>
          <el-button type="primary" @click="handleAdd">
            <el-icon><Plus /></el-icon>新增用户
          </el-button>
        </div>
      </template>

      <el-table :data="tableData" v-loading="loading" stripe>
        <el-table-column prop="username" label="用户名" />
        <el-table-column prop="nickname" label="昵称" />
        <el-table-column prop="email" label="邮箱" />
        <el-table-column prop="phone" label="手机号" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <el-tag :type="row.status === 1 ? 'success' : 'danger'">
              {{ row.status === 1 ? '启用' : '禁用' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="created_at" label="创建时间">
          <template #default="{ row }">
            {{ formatDateTime(row.created_at) }}
          </template>
        </el-table-column>
        <el-table-column label="操作" width="150" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" @click="handleEdit(row)">编辑</el-button>
            <el-button link type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <div class="pagination">
        <el-pagination
          v-model:current-page="query.page"
          v-model:page-size="query.page_size"
          :total="total"
          layout="total, sizes, prev, pager, next"
          @change="fetchData"
        />
      </div>
    </el-card>

    <!-- 新增/编辑弹窗 -->
    <el-dialog v-model="dialogVisible" :title="dialogTitle" width="500px">
      <el-form ref="formRef" :model="form" :rules="rules" label-width="80px">
        <el-form-item label="用户名" prop="username">
          <el-input v-model="form.username" :disabled="!!form.id" />
        </el-form-item>
        <el-form-item label="密码" prop="password" v-if="!form.id">
          <el-input v-model="form.password" type="password" show-password />
        </el-form-item>
        <el-form-item label="昵称" prop="nickname">
          <el-input v-model="form.nickname" />
        </el-form-item>
        <el-form-item label="邮箱" prop="email">
          <el-input v-model="form.email" />
        </el-form-item>
        <el-form-item label="手机号" prop="phone">
          <el-input v-model="form.phone" />
        </el-form-item>
        <el-form-item label="状态" prop="status">
          <el-radio-group v-model="form.status">
            <el-radio :label="1">启用</el-radio>
            <el-radio :label="0">禁用</el-radio>
          </el-radio-group>
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button type="primary" @click="handleSubmit" :loading="submitLoading">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { Plus } from '@element-plus/icons-vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import type { FormInstance, FormRules } from 'element-plus'
import { getUserList, createUser, deleteUser } from '@/api/user'
import type { User } from '@/types'
import { formatDateTime } from '@/utils'

const loading = ref(false)
const tableData = ref<User[]>([])
const total = ref(0)
const query = reactive({
  page: 1,
  page_size: 20
})

const dialogVisible = ref(false)
const dialogTitle = ref('新增用户')
const submitLoading = ref(false)
const formRef = ref<FormInstance>()
const form = reactive<Partial<User>>({
  username: '',
  password: '',
  nickname: '',
  email: '',
  phone: '',
  status: 1
})

const rules: FormRules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}

async function fetchData() {
  loading.value = true
  try {
    const res = await getUserList(query)
    tableData.value = res.items
    total.value = res.total
  } finally {
    loading.value = false
  }
}

function handleAdd() {
  dialogTitle.value = '新增用户'
  dialogVisible.value = true
  Object.assign(form, {
    id: undefined,
    username: '',
    password: '',
    nickname: '',
    email: '',
    phone: '',
    status: 1
  })
}

function handleEdit(row: User) {
  dialogTitle.value = '编辑用户'
  dialogVisible.value = true
  Object.assign(form, { ...row, password: undefined })
}

async function handleDelete(row: User) {
  try {
    await ElMessageBox.confirm('确定要删除该用户吗？', '提示', { type: 'warning' })
    await deleteUser(row.id)
    ElMessage.success('删除成功')
    fetchData()
  } catch {
    // 取消
  }
}

async function handleSubmit() {
  if (!formRef.value) return
  await formRef.value.validate(async (valid) => {
    if (valid) {
      submitLoading.value = true
      try {
        await createUser(form)
        ElMessage.success(form.id ? '更新成功' : '创建成功')
        dialogVisible.value = false
        fetchData()
      } finally {
        submitLoading.value = false
      }
    }
  })
}

onMounted(fetchData)
</script>

<style scoped lang="scss">
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.pagination {
  margin-top: 20px;
  display: flex;
  justify-content: flex-end;
}
</style>
EOF
```

### 步骤 9.8：创建 404 页面

```bash
cat > /data-platform/frontend/src/views/NotFound.vue << 'EOF'
<template>
  <div class="not-found">
    <el-result
      icon="warning"
      title="404"
      sub-title="抱歉，您访问的页面不存在"
    >
      <template #extra>
        <el-button type="primary" @click="$router.push('/')">返回首页</el-button>
      </template>
    </el-result>
  </div>
</template>

<style scoped lang="scss">
.not-found {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}
</style>
EOF
```

---

## 第十阶段：主入口文件

### 步骤 10.1：创建主入口文件

```bash
cat > /data-platform/frontend/src/main.ts << 'EOF'
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import { pinia } from './stores'

// Element Plus
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)

// 注册所有图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.use(pinia)
app.use(router)
app.use(ElementPlus)

app.mount('#app')
EOF
```

### 步骤 10.2：创建根组件

```bash
cat > /data-platform/frontend/src/App.vue << 'EOF'
<template>
  <router-view />
</template>

<style lang="scss">
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #app {
  height: 100%;
  font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB',
    'Microsoft YaHei', '微软雅黑', Arial, sans-serif;
}
</style>
EOF
```

### 步骤 10.3：配置 TypeScript

```bash
cat > /data-platform/frontend/tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
EOF
```

---

## 第十一阶段：构建与部署

### 步骤 11.1：配置生产环境

```bash
cat > /data-platform/frontend/.env.production << 'EOF'
VITE_API_BASE_URL=/api/v1
EOF
```

### 步骤 11.2：构建前端

```bash
cd /data-platform/frontend

# 安装依赖
npm install

# 开发模式启动
npm run dev

# 生产构建（输出到 nginx/html 目录）
npm run build
```

### 步骤 11.3：更新 Nginx 配置（如需独立部署前端）

```bash
# 确保 nginx/html 目录有正确的权限
chmod -R 755 /data-platform/nginx/html

# 重启 nginx 容器
docker compose restart nginx
```

---

## 常用命令

```bash
# 进入前端目录
cd /data-platform/frontend

# 安装依赖
npm install

# 启动开发服务器（端口 5173）
npm run dev

# 生产构建
npm run build

# 代码检查
npm run lint

# 格式化代码
npm run format
```

---

## 功能清单

| 模块 | 功能 | 状态 |
|------|------|------|
| 认证 | 登录/登出 | ✅ |
| 数据概览 | 统计卡片/最近任务 | ✅ |
| 数据源管理 | CRUD/连接测试 | ✅ |
| 采集任务 | 列表/执行任务 | ✅ |
| 数据处理 | 数据清洗/SQL查询 | ✅ |
| 全文检索 | 搜索/高亮 | ✅ |
| 用户管理 | CRUD | ✅ |
