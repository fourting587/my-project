# 数据库详细表结构设计

## 文档信息

- **版本**: v1.0
- **日期**: 2026-04-12
- **数据库**: MySQL 8.0
- **字符集**: utf8mb4_unicode_ci

---

## 目录

1. [数据库设计概述](#1-数据库设计概述)
2. [用户权限模块](#2-用户权限模块)
3. [数据源管理模块](#3-数据源管理模块)
4. [数据采集模块](#4-数据采集模块)
5. [数据存储模块](#5-数据存储模块)
6. [数据处理模块](#6-数据处理模块)
7. [全文检索模块](#7-全文检索模块)
8. [系统管理模块](#8-系统管理模块)

---

## 1. 数据库设计概述

### 1.1 设计原则

- **规范化**: 遵循第三范式，减少数据冗余
- **分表策略**: 大数据表按时间或哈希分片
- **索引优化**: 高频查询字段建立索引
- **软删除**: 使用 `is_deleted` 字段实现逻辑删除
- **时间戳**: 统一使用 `created_at` 和 `updated_at`

### 1.2 命名规范

| 对象 | 规范 | 示例 |
|------|------|------|
| 数据库 | 小写下划线 | `data_platform` |
| 表 | 小写下划线 | `sys_user` |
| 字段 | 小写下划线 | `user_name` |
| 索引 | idx_/uk_前缀 | `idx_user_name` |
| 外键 | fk_前缀 | `fk_user_role` |

### 1.3 通用字段

每个表都包含以下字段:

```sql
`id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
`created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
`is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除: 0-否 1-是',
PRIMARY KEY (`id`)
```

---

## 2. 用户权限模块

### 2.1 用户表 (sys_user)

```sql
CREATE TABLE `sys_user` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名',
    `password` VARCHAR(255) NOT NULL COMMENT '密码(加密存储)',
    `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
    `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
    `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
    `avatar` VARCHAR(500) DEFAULT NULL COMMENT '头像URL',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-启用',
    `last_login_time` DATETIME DEFAULT NULL COMMENT '最后登录时间',
    `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_email` (`email`),
    KEY `idx_phone` (`phone`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统用户表';
```

### 2.2 角色表 (sys_role)

```sql
CREATE TABLE `sys_role` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `role_code` VARCHAR(50) NOT NULL COMMENT '角色编码',
    `role_name` VARCHAR(50) NOT NULL COMMENT '角色名称',
    `description` VARCHAR(255) DEFAULT NULL COMMENT '描述',
    `sort_order` INT DEFAULT 0 COMMENT '排序',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-启用',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_code` (`role_code`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统角色表';
```

**初始化数据:**
```sql
INSERT INTO `sys_role` (`role_code`, `role_name`, `description`) VALUES
('admin', '超级管理员', '拥有所有权限'),
('operator', '操作员', '日常数据操作'),
('viewer', '查看员', '只读权限');
```

### 2.3 用户角色关联表 (sys_user_role)

```sql
CREATE TABLE `sys_user_role` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `role_id` BIGINT UNSIGNED NOT NULL COMMENT '角色ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_role` (`user_id`, `role_id`),
    KEY `fk_user_id` (`user_id`),
    KEY `fk_role_id` (`role_id`),
    CONSTRAINT `fk_sur_user_id` FOREIGN KEY (`user_id`) REFERENCES `sys_user` (`id`),
    CONSTRAINT `fk_sur_role_id` FOREIGN KEY (`role_id`) REFERENCES `sys_role` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户角色关联表';
```

### 2.4 权限表 (sys_permission)

```sql
CREATE TABLE `sys_permission` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `parent_id` BIGINT UNSIGNED DEFAULT 0 COMMENT '父权限ID,0为顶级',
    `perm_code` VARCHAR(100) NOT NULL COMMENT '权限编码',
    `perm_name` VARCHAR(50) NOT NULL COMMENT '权限名称',
    `perm_type` TINYINT NOT NULL COMMENT '权限类型: 1-菜单 2-按钮 3-接口',
    `path` VARCHAR(200) DEFAULT NULL COMMENT '路由路径/接口路径',
    `component` VARCHAR(200) DEFAULT NULL COMMENT '前端组件',
    `icon` VARCHAR(50) DEFAULT NULL COMMENT '图标',
    `sort_order` INT DEFAULT 0 COMMENT '排序',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_perm_code` (`perm_code`),
    KEY `idx_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='权限表';
```

### 2.5 角色权限关联表 (sys_role_permission)

```sql
CREATE TABLE `sys_role_permission` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `role_id` BIGINT UNSIGNED NOT NULL COMMENT '角色ID',
    `permission_id` BIGINT UNSIGNED NOT NULL COMMENT '权限ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_perm` (`role_id`, `permission_id`),
    KEY `fk_role_id` (`role_id`),
    KEY `fk_perm_id` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色权限关联表';
```

---

## 3. 数据源管理模块

### 3.1 数据源表 (datasource)

```sql
CREATE TABLE `datasource` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name` VARCHAR(100) NOT NULL COMMENT '数据源名称',
    `type` VARCHAR(20) NOT NULL COMMENT '类型: mysql/postgresql/sqlserver/api/file',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    
    -- 连接配置(JSON存储,加密敏感信息)
    `host` VARCHAR(200) DEFAULT NULL COMMENT '主机地址',
    `port` INT DEFAULT NULL COMMENT '端口',
    `database` VARCHAR(100) DEFAULT NULL COMMENT '数据库名',
    `username` VARCHAR(100) DEFAULT NULL COMMENT '用户名',
    `password` VARCHAR(255) DEFAULT NULL COMMENT '密码(加密)',
    `params` JSON DEFAULT NULL COMMENT '额外连接参数',
    
    -- API数据源配置
    `api_url` VARCHAR(500) DEFAULT NULL COMMENT 'API地址',
    `api_method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法',
    `api_headers` JSON DEFAULT NULL COMMENT '请求头',
    `api_auth_type` VARCHAR(20) DEFAULT NULL COMMENT '认证类型',
    `api_auth_config` JSON DEFAULT NULL COMMENT '认证配置',
    
    -- 状态
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-启用',
    `last_test_time` DATETIME DEFAULT NULL COMMENT '最后测试时间',
    `last_test_result` TINYINT DEFAULT NULL COMMENT '最后测试结果: 0-失败 1-成功',
    
    `created_by` BIGINT UNSIGNED DEFAULT NULL COMMENT '创建者ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    KEY `idx_type` (`type`),
    KEY `idx_status` (`status`),
    KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据源配置表';
```

---

## 4. 数据采集模块

### 4.1 采集任务表 (collect_task)

```sql
CREATE TABLE `collect_task` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name` VARCHAR(100) NOT NULL COMMENT '任务名称',
    `datasource_id` BIGINT UNSIGNED NOT NULL COMMENT '数据源ID',
    `task_type` VARCHAR(20) NOT NULL COMMENT '任务类型: sql/api/file',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    
    -- 源配置
    `source_config` JSON NOT NULL COMMENT '源配置',
    -- source_config示例:
    -- {
    --   "sql": "SELECT * FROM table WHERE updated_at >= :last_sync",
    --   "primary_key": "id",
    --   "batch_size": 500
    -- }
    
    -- 目标配置
    `target_table` VARCHAR(100) NOT NULL COMMENT '目标表名',
    `field_mapping` JSON DEFAULT NULL COMMENT '字段映射',
    
    -- 调度配置
    `schedule_type` VARCHAR(20) DEFAULT 'manual' COMMENT '调度类型: manual/cron/interval',
    `schedule_config` JSON DEFAULT NULL COMMENT '调度配置',
    -- schedule_config示例:
    -- { "cron": "0 2 * * *" } 或 { "interval": 3600 }
    
    -- 状态统计
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-启用',
    `last_run_time` DATETIME DEFAULT NULL COMMENT '最后执行时间',
    `last_run_status` VARCHAR(20) DEFAULT NULL COMMENT '最后执行状态',
    `success_count` INT DEFAULT 0 COMMENT '成功次数',
    `fail_count` INT DEFAULT 0 COMMENT '失败次数',
    
    `created_by` BIGINT UNSIGNED DEFAULT NULL COMMENT '创建者ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    KEY `idx_datasource_id` (`datasource_id`),
    KEY `idx_task_type` (`task_type`),
    KEY `idx_status` (`status`),
    KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据采集任务表';
```

### 4.2 采集任务实例表 (collect_instance)

```sql
CREATE TABLE `collect_instance` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `task_id` BIGINT UNSIGNED NOT NULL COMMENT '任务ID',
    `instance_no` VARCHAR(50) NOT NULL COMMENT '实例编号',
    
    -- 执行配置
    `sync_mode` VARCHAR(20) DEFAULT 'incremental' COMMENT '同步模式: full/incremental',
    `start_time` DATETIME DEFAULT NULL COMMENT '数据开始时间',
    `end_time` DATETIME DEFAULT NULL COMMENT '数据结束时间',
    
    -- 执行状态
    `status` VARCHAR(20) NOT NULL COMMENT '状态: pending/running/completed/failed/cancelled',
    `progress` INT DEFAULT 0 COMMENT '进度百分比',
    
    -- 执行统计
    `total_count` BIGINT DEFAULT 0 COMMENT '总记录数',
    `success_count` BIGINT DEFAULT 0 COMMENT '成功数',
    `fail_count` BIGINT DEFAULT 0 COMMENT '失败数',
    `start_at` DATETIME DEFAULT NULL COMMENT '开始时间',
    `end_at` DATETIME DEFAULT NULL COMMENT '结束时间',
    `duration_ms` INT DEFAULT NULL COMMENT '执行耗时(毫秒)',
    
    -- 错误信息
    `error_msg` TEXT DEFAULT NULL COMMENT '错误信息',
    
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_instance_no` (`instance_no`),
    KEY `idx_task_id` (`task_id`),
    KEY `idx_status` (`status`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='采集任务实例表';
```

### 4.3 采集日志表 (collect_log)

```sql
CREATE TABLE `collect_log` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `instance_id` BIGINT UNSIGNED NOT NULL COMMENT '实例ID',
    `level` VARCHAR(10) NOT NULL COMMENT '日志级别: debug/info/warning/error',
    `message` TEXT NOT NULL COMMENT '日志内容',
    `details` JSON DEFAULT NULL COMMENT '详细信息',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_instance_id` (`instance_id`),
    KEY `idx_level` (`level`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='采集日志表';
```

---

## 5. 数据存储模块

### 5.1 数据表元信息表 (data_table_meta)

```sql
CREATE TABLE `data_table_meta` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `table_name` VARCHAR(100) NOT NULL COMMENT '表名',
    `table_comment` VARCHAR(200) DEFAULT NULL COMMENT '表注释',
    `table_type` VARCHAR(20) NOT NULL COMMENT '表类型: source/processed/result',
    `source_type` VARCHAR(20) DEFAULT NULL COMMENT '来源类型: collect/import/manual',
    `source_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '来源ID',
    
    -- 统计信息
    `row_count` BIGINT DEFAULT 0 COMMENT '行数',
    `size_bytes` BIGINT DEFAULT 0 COMMENT '大小(字节)',
    `last_analyze_time` DATETIME DEFAULT NULL COMMENT '最后分析时间',
    
    -- 生命周期
    `retention_days` INT DEFAULT 60 COMMENT '保留天数',
    `expire_at` DATETIME DEFAULT NULL COMMENT '过期时间',
    
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_table_name` (`table_name`),
    KEY `idx_table_type` (`table_type`),
    KEY `idx_expire_at` (`expire_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据表元信息表';
```

### 5.2 数据表字段元信息表 (data_table_column)

```sql
CREATE TABLE `data_table_column` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `table_id` BIGINT UNSIGNED NOT NULL COMMENT '表ID',
    `column_name` VARCHAR(100) NOT NULL COMMENT '字段名',
    `column_comment` VARCHAR(200) DEFAULT NULL COMMENT '字段注释',
    `data_type` VARCHAR(50) NOT NULL COMMENT '数据类型',
    `is_nullable` TINYINT DEFAULT 1 COMMENT '是否可空',
    `is_primary` TINYINT DEFAULT 0 COMMENT '是否主键',
    `default_value` VARCHAR(255) DEFAULT NULL COMMENT '默认值',
    `sort_order` INT DEFAULT 0 COMMENT '排序',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_table_id` (`table_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据表字段元信息表';
```

### 5.3 业务数据表示例 - 订单表 (biz_orders_0 ~ biz_orders_15)

```sql
-- 分表设计: 16张表按用户ID取模分片
CREATE TABLE `biz_orders_0` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `order_no` VARCHAR(50) NOT NULL COMMENT '订单编号',
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `product_id` BIGINT UNSIGNED NOT NULL COMMENT '商品ID',
    `product_name` VARCHAR(200) NOT NULL COMMENT '商品名称',
    `category` VARCHAR(50) DEFAULT NULL COMMENT '商品类别',
    `quantity` INT NOT NULL DEFAULT 1 COMMENT '数量',
    `unit_price` DECIMAL(10,2) NOT NULL COMMENT '单价',
    `total_amount` DECIMAL(12,2) NOT NULL COMMENT '总金额',
    `discount_amount` DECIMAL(10,2) DEFAULT 0 COMMENT '优惠金额',
    `pay_amount` DECIMAL(12,2) NOT NULL COMMENT '实付金额',
    `status` TINYINT NOT NULL DEFAULT 0 COMMENT '订单状态: 0-待支付 1-已支付 2-处理中 3-已发货 4-已完成 5-已取消',
    `pay_time` DATETIME DEFAULT NULL COMMENT '支付时间',
    `ship_time` DATETIME DEFAULT NULL COMMENT '发货时间',
    `receive_time` DATETIME DEFAULT NULL COMMENT '收货时间',
    `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_product_id` (`product_id`),
    KEY `idx_category` (`category`),
    KEY `idx_status` (`status`),
    KEY `idx_created_at` (`created_at`),
    KEY `idx_pay_time` (`pay_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='订单表-分片0';
```

**分表规则:**
```python
# 根据user_id取模确定分表
shard = user_id % 16
table_name = f"biz_orders_{shard}"
```

---

## 6. 数据处理模块

### 6.1 数据处理任务表 (process_task)

```sql
CREATE TABLE `process_task` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name` VARCHAR(100) NOT NULL COMMENT '任务名称',
    `process_type` VARCHAR(20) NOT NULL COMMENT '处理类型: clean/transform/aggregate/import/export',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    
    -- 输入输出配置
    `input_config` JSON NOT NULL COMMENT '输入配置',
    `output_config` JSON NOT NULL COMMENT '输出配置',
    -- input_config示例:
    -- { "type": "table", "table": "raw_data", "filters": {...} }
    -- output_config示例:
    -- { "type": "table", "table": "clean_data", "mode": "append" }
    
    -- 处理规则
    `process_rules` JSON DEFAULT NULL COMMENT '处理规则',
    
    -- 调度配置
    `schedule_type` VARCHAR(20) DEFAULT 'manual' COMMENT '调度类型',
    `schedule_config` JSON DEFAULT NULL COMMENT '调度配置',
    
    -- 状态
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `last_run_time` DATETIME DEFAULT NULL COMMENT '最后执行时间',
    `last_run_status` VARCHAR(20) DEFAULT NULL COMMENT '最后执行状态',
    
    `created_by` BIGINT UNSIGNED DEFAULT NULL COMMENT '创建者ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (`id`),
    KEY `idx_process_type` (`process_type`),
    KEY `idx_status` (`status`),
    KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据处理任务表';
```

### 6.2 数据处理实例表 (process_instance)

```sql
CREATE TABLE `process_instance` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `task_id` BIGINT UNSIGNED NOT NULL COMMENT '任务ID',
    `instance_no` VARCHAR(50) NOT NULL COMMENT '实例编号',
    
    -- 执行统计
    `input_count` BIGINT DEFAULT 0 COMMENT '输入记录数',
    `output_count` BIGINT DEFAULT 0 COMMENT '输出记录数',
    `success_count` BIGINT DEFAULT 0 COMMENT '成功数',
    `fail_count` BIGINT DEFAULT 0 COMMENT '失败数',
    
    -- 执行状态
    `status` VARCHAR(20) NOT NULL COMMENT '状态',
    `progress` INT DEFAULT 0 COMMENT '进度',
    `start_at` DATETIME DEFAULT NULL COMMENT '开始时间',
    `end_at` DATETIME DEFAULT NULL COMMENT '结束时间',
    `duration_ms` INT DEFAULT NULL COMMENT '耗时(毫秒)',
    
    -- 文件信息(导入/导出)
    `file_name` VARCHAR(255) DEFAULT NULL COMMENT '文件名',
    `file_size` BIGINT DEFAULT NULL COMMENT '文件大小',
    `file_path` VARCHAR(500) DEFAULT NULL COMMENT '文件路径',
    
    -- 错误信息
    `error_msg` TEXT DEFAULT NULL COMMENT '错误信息',
    
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_instance_no` (`instance_no`),
    KEY `idx_task_id` (`task_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据处理实例表';
```

---

## 7. 全文检索模块

### 7.1 搜索索引配置表 (search_index)

```sql
CREATE TABLE `search_index` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `index_name` VARCHAR(100) NOT NULL COMMENT '索引名称',
    `index_alias` VARCHAR(100) DEFAULT NULL COMMENT '索引别名',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    
    -- 数据源配置
    `source_type` VARCHAR(20) NOT NULL COMMENT '源类型: table/api',
    `source_config` JSON NOT NULL COMMENT '源配置',
    -- source_config示例:
    -- { "table": "articles", "primary_key": "id", "update_field": "updated_at" }
    
    -- 字段映射
    `field_mappings` JSON NOT NULL COMMENT '字段映射配置',
    -- field_mappings示例:
    -- [
    --   { "field": "title", "type": "text", "analyzer": "ik_smart", "boost": 2 },
    --   { "field": "content", "type": "text", "analyzer": "ik_smart" },
    --   { "field": "author", "type": "keyword" }
    -- ]
    
    -- 同步配置
    `sync_type` VARCHAR(20) DEFAULT 'manual' COMMENT '同步类型: manual/realtime/schedule',
    `sync_config` JSON DEFAULT NULL COMMENT '同步配置',
    
    -- 统计
    `doc_count` BIGINT DEFAULT 0 COMMENT '文档数量',
    `last_sync_time` DATETIME DEFAULT NULL COMMENT '最后同步时间',
    
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_index_name` (`index_name`),
    KEY `idx_source_type` (`source_type`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='搜索索引配置表';
```

### 7.2 搜索日志表 (search_log)

```sql
CREATE TABLE `search_log` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '用户ID',
    `index_name` VARCHAR(100) NOT NULL COMMENT '索引名称',
    `query` TEXT NOT NULL COMMENT '查询关键词',
    `filters` JSON DEFAULT NULL COMMENT '过滤条件',
    `result_count` INT DEFAULT 0 COMMENT '返回结果数',
    `took_ms` INT DEFAULT NULL COMMENT '耗时(毫秒)',
    `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
    `user_agent` VARCHAR(500) DEFAULT NULL COMMENT 'User-Agent',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_index_name` (`index_name`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='搜索日志表';
```

---

## 8. 系统管理模块

### 8.1 系统配置表 (sys_config)

```sql
CREATE TABLE `sys_config` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
    `config_value` TEXT DEFAULT NULL COMMENT '配置值',
    `config_type` VARCHAR(20) DEFAULT 'string' COMMENT '值类型: string/int/float/json/boolean',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    `is_system` TINYINT DEFAULT 0 COMMENT '是否系统配置',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';
```

**初始化数据:**
```sql
INSERT INTO `sys_config` (`config_key`, `config_value`, `config_type`, `description`, `is_system`) VALUES
('site.name', '数据处理平台', 'string', '站点名称', 1),
('site.logo', '/logo.png', 'string', '站点Logo', 1),
('data.retention_days', '60', 'int', '数据保留天数', 1),
('data.batch_size', '500', 'int', '默认批次大小', 1),
('upload.max_size', '104857600', 'int', '上传文件最大字节数', 1),
('upload.allowed_types', '["csv","xlsx","xls","json"]', 'json', '允许上传的文件类型', 1);
```

### 8.2 操作日志表 (sys_operation_log)

```sql
CREATE TABLE `sys_operation_log` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '用户ID',
    `username` VARCHAR(50) DEFAULT NULL COMMENT '用户名',
    `module` VARCHAR(50) NOT NULL COMMENT '功能模块',
    `action` VARCHAR(50) NOT NULL COMMENT '操作类型',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '操作描述',
    `request_method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法',
    `request_url` VARCHAR(500) DEFAULT NULL COMMENT '请求URL',
    `request_params` JSON DEFAULT NULL COMMENT '请求参数',
    `response_code` INT DEFAULT NULL COMMENT '响应状态码',
    `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
    `user_agent` VARCHAR(500) DEFAULT NULL COMMENT 'User-Agent',
    `execute_time_ms` INT DEFAULT NULL COMMENT '执行时长(毫秒)',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_module` (`module`),
    KEY `idx_action` (`action`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='操作日志表';
```

### 8.3 定时任务表 (sys_scheduled_task)

```sql
CREATE TABLE `sys_scheduled_task` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `task_name` VARCHAR(100) NOT NULL COMMENT '任务名称',
    `task_key` VARCHAR(50) NOT NULL COMMENT '任务标识',
    `cron_expression` VARCHAR(100) NOT NULL COMMENT 'Cron表达式',
    `execute_class` VARCHAR(200) DEFAULT NULL COMMENT '执行类',
    `execute_params` JSON DEFAULT NULL COMMENT '执行参数',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '描述',
    
    -- 执行状态
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用 1-启用',
    `last_execute_time` DATETIME DEFAULT NULL COMMENT '最后执行时间',
    `last_execute_result` TINYINT DEFAULT NULL COMMENT '最后执行结果',
    `execute_count` INT DEFAULT 0 COMMENT '执行次数',
    `fail_count` INT DEFAULT 0 COMMENT '失败次数',
    
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_task_key` (`task_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='定时任务表';
```

---

## 附录

### A. 数据库初始化脚本

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS data_platform 
    DEFAULT CHARACTER SET utf8mb4 
    DEFAULT COLLATE utf8mb4_unicode_ci;

USE data_platform;

-- 执行上述所有建表语句

-- 初始化角色数据
INSERT INTO `sys_role` (`role_code`, `role_name`, `description`) VALUES
('admin', '超级管理员', '拥有所有权限'),
('operator', '操作员', '日常数据操作'),
('viewer', '查看员', '只读权限');

-- 初始化管理员用户(密码: Admin@123)
INSERT INTO `sys_user` (`username`, `password`, `nickname`, `email`, `status`) VALUES
('admin', '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/X4.VTtYA.qGZvKG6G', '系统管理员', 'admin@example.com', 1);

-- 关联管理员角色
INSERT INTO `sys_user_role` (`user_id`, `role_id`) 
SELECT 1, id FROM `sys_role` WHERE `role_code` = 'admin';

-- 初始化系统配置
INSERT INTO `sys_config` (`config_key`, `config_value`, `config_type`, `description`, `is_system`) VALUES
('site.name', '数据处理平台', 'string', '站点名称', 1),
('data.retention_days', '60', 'int', '数据保留天数', 1),
('data.batch_size', '500', 'int', '默认批次大小', 1);
```

### B. 索引优化建议

| 表名 | 建议索引 | 说明 |
|------|----------|------|
| biz_orders_* | idx_created_at + idx_status | 时间范围查询 + 状态筛选 |
| collect_instance | idx_task_id + idx_created_at | 任务历史查询 |
| sys_operation_log | idx_created_at | 日志清理和查询 |
| search_log | idx_created_at | 定期清理 |

### C. 分表策略

```sql
-- 创建分表的存储过程
DELIMITER $$

CREATE PROCEDURE create_shard_tables()
BEGIN
    DECLARE i INT DEFAULT 0;
    DECLARE table_name VARCHAR(50);
    
    WHILE i < 16 DO
        SET table_name = CONCAT('biz_orders_', i);
        SET @sql = CONCAT('
            CREATE TABLE IF NOT EXISTS `', table_name, '` (
                `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
                `order_no` VARCHAR(50) NOT NULL,
                `user_id` BIGINT UNSIGNED NOT NULL,
                `product_id` BIGINT UNSIGNED NOT NULL,
                `product_name` VARCHAR(200) NOT NULL,
                `category` VARCHAR(50) DEFAULT NULL,
                `quantity` INT NOT NULL DEFAULT 1,
                `unit_price` DECIMAL(10,2) NOT NULL,
                `total_amount` DECIMAL(12,2) NOT NULL,
                `discount_amount` DECIMAL(10,2) DEFAULT 0,
                `pay_amount` DECIMAL(12,2) NOT NULL,
                `status` TINYINT NOT NULL DEFAULT 0,
                `pay_time` DATETIME DEFAULT NULL,
                `ship_time` DATETIME DEFAULT NULL,
                `receive_time` DATETIME DEFAULT NULL,
                `remark` VARCHAR(500) DEFAULT NULL,
                `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
                `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
                PRIMARY KEY (`id`),
                UNIQUE KEY `uk_order_no` (`order_no`),
                KEY `idx_user_id` (`user_id`),
                KEY `idx_product_id` (`product_id`),
                KEY `idx_category` (`category`),
                KEY `idx_status` (`status`),
                KEY `idx_created_at` (`created_at`)
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
        ');
        PREPARE stmt FROM @sql;
        EXECUTE stmt;
        DEALLOCATE PREPARE stmt;
        SET i = i + 1;
    END WHILE;
END$$

DELIMITER ;

-- 执行创建分表
CALL create_shard_tables();
```
